import {SearchIcon, SearchInput, SearchWrapper} from "./styled";

const Search = () => {
  return (
    <SearchWrapper>
      <SearchIcon/>
      <SearchInput/>
    </SearchWrapper>
  )
};

export default Search;